#include <stdio.h>

/**
 * main - is a function that print its name
 *
 * @argc: input
 * @argv: input
 *
 * Return: the function name
 */

int main(int __attribute__((unused))argc, char *argv[])
{

	printf("%s\n", argv[0]);
	return (0);
}
