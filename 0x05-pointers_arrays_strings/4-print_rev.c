#include "main.h"

/**
 * print_rev - is a function that prints a string in reverse
 *
 * @s: string
 */

void print_rev(char *s)
{

	while (s[rtt] != '\0')
	{
		rtt++;

		while (rtt--)
		{
			_putchar (s[rtt]);
		}
		_putchar('\n');
	}
}
