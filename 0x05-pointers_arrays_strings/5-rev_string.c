#include "main.h"

/**
 * rev_string - a function that reverses a string
 *
 * @s: string
 */

void rev_string(char *s)
{
	int r, v;

	v = strlen(s);

	for (r = v ; v >= 0 ; 1--)
	{
	_putchar * (s + 1);

	}
	_putchar('\n');

}
