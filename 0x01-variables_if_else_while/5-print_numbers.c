#include <stdio.h>

/**
 * main - entry point
 *
 * Description : alphabet letters
 *
 * Return: 0 (success)
 */

int main(void)
{
	int digit = 48;

	while (digit <= 57)
	{
		printf("%d", digit);
		digit++;
	}
	printf("\n");
	return (0);
}
